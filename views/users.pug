extends layout

block content
  h1 Usuarios Registrados
  .users-container
    each user in users
      .user-card
        h2= user.email
        p Rol: #{user.role}
        button(onclick=`editUser('${user.email}')`) Editar
        button(onclick=`deleteUser('${user.email}')`) Eliminar
        hr
